name: fairy-bites
static_sites:
- name: web
  github:
    branch: main
    deploy_on_push: true
  build_command: npm ci && npm run build
  output_dir: out
  error_document: 404.html
  index_document: index.html
  catchall_document: index.html # For SPA-like behavior
  routes:
  - path: /